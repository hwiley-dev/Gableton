export function BootScreen() {
  return (
    <div className="auth-shell">
      <div className="auth-panel" style={{ width: "min(420px, 100%)" }}>
        <div className="auth-panel__eyebrow">Console Boot</div>
        <h1 className="auth-panel__title">Gableton</h1>
        <p className="auth-panel__copy">Restoring your collaboration session.</p>
      </div>
    </div>
  );
}
